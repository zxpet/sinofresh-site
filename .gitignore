# 大体积过程产物（留在本地，不进仓库）
/_backup/
/_db_backup/
/screenshots/
/ai-images-raw/
/.workbuddy/

# 主题内的批次备份。原来的 /_backup/ 是根目录锚定的，匹配不到
# sinofresh-theme/_backup/ —— 于是 93 个批次备份全被提交进了仓库
# （348 个文件、22.86MB），每次 push 都要重新传一遍。这条规则从下一批
# 开始止血；已入库的历史文件需要单独的 git rm -r --cached 清理。
**/_backup/

# 根目录一次性截图/草稿
/*.png

# 系统与临时文件
.DS_Store
._*
__pycache__/
node_modules/
# 迁移包：只忽略大文件（tar/sql），部署脚本与说明要进仓库
# （deploy.sh 是上线生产要复用的脚本，2026-09-21 已加包内容守门）
/_migration/*
!/_migration/README.md
!/_migration/deploy.sh
!/_migration/nginx-dev.zxpet.com.conf
