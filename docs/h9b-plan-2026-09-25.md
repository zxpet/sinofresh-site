# H9b 方案定稿 — 表单选项文字后台可编辑（2026-09-25）

裁决记录：专属 select 不进（FF 编辑器原生改）；Document Type checkbox 不纳入；Dosage Form 列表独立 textarea 手改。

## 一、改动清单

1. **`inc/formula-admin.php`**
   - register_setting：`sf_form_options`（`sf_site_settings` group），sanitize 为三个数组的关联数组，`dosage_forms` / `countries` / `target_markets` 各为字符串数组（textarea 每行一项，trim 后去空行）
   - `add_submenu_page`：'Form Options'（slug `sf-form-options`），渲染三组 textarea；hint 写明「每行一项；改一处，询盘/样品/参观/COA 表单同步；清空＝回退表单现值」
   - 缺省值 unset ⇒ 空数组（empty-means-absent）
2. **`functions.php`**
   - 新函数 `sinofresh_form_options_for($prefix)`：读 `sf_form_options`，按前缀返回列表或 null
   - 挂 `fluentform/rendering_field_data_select` 过滤器：字段 name 前缀匹配 `country_` / `target_market_` / `interested_dosage_form_`，命中且对应列表非空 ⇒ 重写 `$data['settings']['advanced_options']`（value=label 同值）；列表空或未命中 ⇒ 原样返回
   - 静态默认：无 option 时前台字节与现状零差异（表单现值兜底）
3. **版本**：2.10.84 → **2.10.85**（style.css 头＋enqueue，3 处同步；本次不动 CSS）
4. **门 `tools/h9b_gate.py`**（新）
   - --source：过滤器注册存在、前缀映射三键在位、缺省不覆盖、admin 页 3 textarea 注册、版本 2.10.85
   - --preflight：① 带头渲染 form 8/9/11 页面，select 选项＝Site Settings 缺省注入值；② 写入 option 探针（改 countries 为含 probe 项的列表）⇒ 4 表单 Country 选项同步出现 probe 项；③ 清 option ⇒ 逐字节回退表单现值
   - --live：pull 后同 ②③（最小化 DB 写，探针即写即删）

## 二、影响面

- 前台：缺省状态下**零字节漂移**（无 option ＝ 表单现值）；运营填写后按填写渲染
- 数据：新增 1 条 option；FF form_fields 不改（表单定义原样保留，回滚＝删 option）
- 风险点：name 前缀匹配（`country_*` 序号不稳定）已在方案中锚定；select 提交值不做列表校验（FF 默认），与现状一致

## 三、流程

代码 → bump → 门（--source/--preflight）→ 截图（后台 Form Options 页＋前台表单下拉展开）→ 报告 → 停下等 pull。
