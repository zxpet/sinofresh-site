# H10 批次报告 — 产品参数区后台化（2026-09-25）

**状态：commit `bc6d336` 已推送。dev live 未动（`9bbd97e`），等你授权 pull。生产零接触。**
preflight 副本已含全部改动（admin/前台/REST 全链路实测）。

---

## 一、交付内容

### 1. 剂型级四事实 → `sf_form_facts` option（裁决 ③ 单点改源）

- 新 Site Settings 子页 **Dosage Form Facts**：8 剂型 × 4 字段（MOQ / Lead time / Certifications / Packaging formats），中文说明＋默认值展示，清空＝回退默认
- **单点改源＝`sinofresh_formula_spec_cell()` 本体改源**（option 优先、默认兜底）：五个消费点——剂型页 facts-mini 带、hero meta、intro 从句、FAQ 答案、Product JSON-LD——全部仍走这一个读取函数，各消费点零改动。改一行设置，**8 个剂型页＋该剂型全部详情页同步变**
- 8 个模板的静态带换成 `[SF_FACTS_MINI <slug>]` marker，`render_block` 过滤器换成从 option 构建的带
- Certifications 保持两层链：Site Settings → Certifications 优先，per-form 字段仅兜底

### 2. Spec Sheet 字段组（裁决 ①②）

- sf-mb 新组 **Spec Sheet**：14 个 `sf_param_*` 可选字段（Sample Policy / Inactive Ingredients / Calorie Content / Nutritional Adequacy / Compliance Markets / Label Language / Label Items / Customizable / Private Label / Payment Terms / Primary Packaging / Gross-Net Weight / Container Load / Storage），中文 hint 仅后台渲染
- 158 五键（weight/counts/shape/species/lifestage）hint 尾部已加「疑似测试残留：全池值，填内容阶段逐条核对」标注；**数据未动**（裁决 ④）

### 3. 前台参数表分组渲染

- 三个命名组：**Core Parameters / Product Specifications / Packaging & Logistics**（组内 empty-means-absent 契约不变；Lead Time／Primary Packaging 记录值优先、剂型事实兜底）
- **Factory & Trust 独立带**（裁决：视觉独立、有分隔）：spec sheet 下方独立 section＋顶部分隔线；7 个 `sf_trust_*` option 在 Site Settings → Factory Information；全空整组不渲染；**Annual Capacity / On-time Delivery / Reorder Rate 故意空着上线**（无真实数字不编造）
- Primary Packaging 留空时回退剂型 Packaging formats；cartons 数据继续在正文带渲染（不重复加行）

### 4. 占位值（裁决 ⑤，按批准清单已写入）

- **A 类（站内已核文案）**：8 剂型四事实 defaults＝模板现值逐字符；Factory Size 15,000㎡ / Cleanroom ISO 8 / Export Markets 30+ countries（factory-tour statbar 原值）
- **B 类（行业常规 7 项）**：Storage / Label Language / Label Items / Customizable / Private Label / Sample Policy / Response Time（Within 24 hours）——已写入 defaults
- **C 类留空不渲染**：Inactive Ingredients / Calorie / Adequacy / Compliance Markets / Payment Terms / 物流数值 / Annual Capacity / On-time / Reorder

## 二、验证

| 门 | 结果 |
|---|---|
| `h10_gate.py --source` | **30/30**（单源无第二读取路径、8 模板 marker、零中文、14 字段、158 标注×5、版本 2.10.83） |
| `h10_gate.py --preflight` | **19/19**（P1 字节零漂移 ×8、P2 三组＋信任带＋空行不渲染＋前台零中文、P3 JSON-LD live vs preflight 零漂移、P4 option 覆盖探针改一处→剂型页＋详情页同步→删除还原） |
| h9 --source / b3bc --local | 34 ok＋2 红 / 56＋2 红＝版本钉 2.10.82，**by design** |
| h9 --preflight A/B | 把 preflight 副本暂回 HEAD 重跑：**同样 18 ok / 6 FAIL，红清单逐条一致** ⇒ H10 引入 0 个新红（5 个＝h9 门钉 158 迁移前数据态；1 个＝节流 429 拦截门自身的连跑提交） |

截图 6 张在 `docs/h10-shots/`：admin Spec Sheet 组 / Dosage Form Facts / Factory Information＋前台 specs 1440/375 / trust 1440。

## 三、必须知道的两件事

1. **预存在分歧被单源化纠正**：8 个模板硬编码的 Certifications 是旧值 `BRC`，而 Site Settings 证书行（所有详情页 spec sheet 一直打印的）是 `BRCGS`。动态带按单源设计跟随 Site Settings ⇒ **切上去后剂型页 Certifications 从 BRC 变 BRCGS**。这是数据被纠正而非代码漂移，其余三行与 HEAD 逐字节相同（门 P1 逐字断言）。
2. **改一处、8 页同步**（核对时请知悉）：在 Site Settings → Dosage Form Facts 改某剂型任一字段，8 个剂型页的 facts-mini 带＋该剂型所有详情页的 hero meta／intro 从句／FAQ 答案／spec sheet MOQ 行／Product JSON-LD **同时变**——这是单读取函数的设计结果，不是巧合。
3. 提醒（你要求的）：Label Language 与 Response Time（Within 24 hours）是对外承诺，内容核对阶段请确认可兑现；Response Time 当前与 factory-tour 页「within 24 hours」口径一致。

## 四、pull 授权后的动作（顺序）

1. dev `git pull --ff-only`（`9bbd97e → bc6d336`；无迁移、无数据写入、无新 post meta）
2. `h10_gate.py --live`（约 60s；P4 会临时写/删 sf_form_facts option 并还原）
3. 抽查一个剂型页 Certifications＝BRCGS（预期变化）

无版本外资产变化；style.css 2.10.83、functions.php enqueue 2.10.83 已同步。

## 五、待办与后续

- **runbook 方案 A 待命**（`docs/golive-runbook-2026-09-25.md`，四点裁决已收、旧站无业务数据已确认）——H10 上线后重启，D3 清除/保留表清单将先给你过目
- 填内容阶段：C 类字段逐条填、158 五键全池值核对、Label Language / Response Time 承诺确认
