# H10b 修正方案 — 参数区拆分类别（2026-09-25，待确认）

> 前置：`docs/h10b-scan-2026-09-25.md`。四项模糊裁决已收：Customizable 保留 / Packaging formats 保留字段只删参数表行 / Response Time 留信任带 / Main Ingredients 行保留。
> **未动任何代码/数据，本方案等你确认后实施。**

## 一、最终去留表

**参数区保留**：sf_form_facts 全部 4 项（MOQ/Lead time/Certifications/Packaging formats）＋ sf_param 的 Sample Policy / Customizable / Private Label / Payment Terms ＋ sf_trust 全部 7 项 ＋ 既有行（Dosage Form/Applicable Pet/Shelf Life/Place of Origin/OEM-ODM/Main Ingredients/Life Stage/Shape/Unit Weight/Pack Size）。

**拆掉归正文（10 个 sf_param 键）**：Inactive Ingredients / Calorie Content / Nutritional Adequacy（配方信息）；Compliance Markets / Label Language / Label Items Available（标签合规）；Primary Packaging / Gross-Net Weight / Container Load / Storage（包装物流）。

## 二、改动清单

1. **functions.php — `sinofresh_formula_specs_table()`**：
   - spec_rows 循环（~3101-3116）删 6 键，留 3 键（customizable/private_label/payment_terms）
   - pack_rows 整段（~3119-3139）删＋`$groups` 删 packaging 条目 ⇒ **前台三组变两组**（Core Parameters / Product Specifications）；组渲染循环与 CSS 零改动（空组自动跳过）
   - 注释同步（12 行表格的历史叙述、H10 段落）
2. **inc/formula-admin.php — specsheet 组缩编 14→4**；`sf-param-types` 白名单同步删 10 键；hint 相应清理
3. **sf_form_facts / spec_cell / facts-mini / JSON-LD：零改动**（裁决：Packaging formats 保留字段）
4. **Trust 带：零改动**（Response Time 留位）
5. **数据**：删除 158 的 5 行拆掉键测试 meta（inactive_ingredients〔垃圾串〕/calorie/adequacy/compliance_markets/label_language）；其余 sf_param 无值；customizable 的 158 行保留；**不做批量 meta 清理**（其余键 DB 本来就空）
6. **h10_gate.py 就地演进**：
   - --source：14 字段断言改 4 字段＋10 键「不可再注册」反向断言
   - P2：三组断言改两组＋「Packaging & Logistics 组标题不出现」＋拆掉行 label 前台不可见（visible-text 剥离后搜，防head内联CSS假命中）
   - P1：字节零漂移改造为「保留行零漂移＋删行不存在」（删行是预期变化，旧 P1 全页基线作废重钉）
   - 旧门按惯例「不新增红」，Main Ingredients 未拆 ⇒ b2d_h7c 系不受影响；实施时 A/B 取证确认
7. **版本**：style.css 2.10.83→2.10.84，functions.php enqueue 同步，工具侧 6 文件同步

## 三、预期前台变化（验收时知悉）

- 每个详情页：**Packaging & Logistics 组整组消失**（之前该组恒有 Primary Packaging 行＝剂型兜底值）；Product Specifications 组缩为有值行（多数帖只剩 Main Ingredients±Life Stage 等）
- Core Parameters 组与 Factory & Trust 带：零变化
- 剂型列表页（facts-mini 带）与 JSON-LD：零变化
- 后台：Spec Sheet 组从 14 字段变 4 字段

## 四、执行顺序（确认后）

代码（functions.php / formula-admin.php）→ 158 测试 meta 清理 → 版本 bump 6 文件 → h10_gate --source / --preflight → 截图（前后台）→ 批次报告 → **停下等 pull 授权**。
